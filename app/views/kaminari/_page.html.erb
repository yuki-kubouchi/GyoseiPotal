<li class="page-item">
  <% if page.current? %>
    <span class="px-3 py-1 border rounded bg-blue-500 text-white"><%= page.display_tag %></span>
  <% else %>
    <%= link_to page.display_tag, url_for(page.params), class: "px-3 py-1 border rounded hover:bg-gray-100" %>
  <% end %>
</li>
